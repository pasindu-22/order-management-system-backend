server:
  port: 8081

spring:
  security:
    user:
      name: admin
      password: admin123
  application:
    name: inventory-service  # This is the name that will be used in Eureka
  datasource:
    url: jdbc:mysql://localhost:3306/inventory_service_db
    username: devuser
    password: Pasindu119
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect

  kafka:
    bootstrap-servers: localhost:9092   # https://docs.docker.com/guides/kafka/  Used to run Kafka in Docker
    consumer:
      group-id: inventory-group
      auto-offset-reset: earliest
    producer:
      acks: all

redisson:
  config: |
    singleServerConfig:
      address: "redis://localhost:6379"

