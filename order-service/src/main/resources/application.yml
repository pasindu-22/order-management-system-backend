server:
  port: 8082

spring:
  application:
    name: order-service  # This is the name that will be used in Eureka
  datasource:
    url: jdbc:mysql://localhost:3306/order_service_db
    username: devuser
    password: Pasindu119
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect
